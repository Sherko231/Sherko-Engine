# Sherko Engine Decision Log

This is the durable, append-only summary of accepted engineering decisions. It records why the repository is shaped this way without mixing in live task status. New entries receive the next ID; superseded entries remain and link to the replacement.

## Status values

- `Accepted` — binding unless deliberately superseded.
- `Provisional` — usable for current work but requires a named validation gate.
- `Superseded` — historical; a later decision controls.

## Decisions

| ID | Status | Decision | Rationale / consequence |
| --- | --- | --- | --- |
| D-001 | Accepted | Authored engine and game runtime source is Java 25 only; Gradle Kotlin DSL is allowed for build configuration. | Preserves the Java-only product constraint without a no-value build-script migration. |
| D-002 | Accepted | Windows x64 and Steam are the initial release platform/social layer. | Keeps native verification and packaging bounded for v1. |
| D-003 | Accepted | OpenGL 4.6 Core with a forward renderer is the first renderer. | Smaller v1 implementation surface than Vulkan; multiple backends remain deferred. |
| D-004 | Accepted | Jolt through Jolt JNI supplies rigid-body physics; native objects use explicit ownership/cleanup. | Avoids building a solver and prevents treating GC as native-resource management. |
| D-005 | Accepted | Multiplayer uses server-authoritative simulation, not deterministic physics lockstep. | Physics-heavy shared objects are corrected/replicated from the server; clients do not own arbitrary authoritative transforms. |
| D-006 | Accepted | Production networking uses transport-neutral APIs with direct-IP and Steam adapters; no home-grown reliable UDP transport. | Keeps gameplay independent of transport and limits custom protocol risk. |
| D-007 | Provisional | Java FFM may call the Steam flat API without authored C/C++ glue. | API/pointer access is proven; P0-T09A / Issue #42 must prove end-to-end connection, callbacks, message release, and cleanup before production use. |
| D-008 | Accepted | Runtime game UI is an engine-owned retained, renderer-neutral model; imgui-java is editor/debug only. | Prevents shipped UI/game state from coupling to OpenGL or internal tooling and includes keyboard/controller navigation. |
| D-009 | Accepted | glTF 2.0 is authoring interchange; gameplay loads cooked engine formats. | Separates flexible import from bounded/versioned runtime data. |
| D-010 | Accepted | Disk/network formats use explicit versioned codecs; Java object serialization is forbidden. | Provides bounded compatibility and avoids unsafe, implicit layouts. |
| D-011 | Accepted | v1 gameplay is first-person; third-person presentation follows a stable multiplayer vertical slice. | Core player/authority contracts stay camera-agnostic without blocking the first playable loop. |
| D-012 | Accepted | Target is 1–4 supported players, with intended co-op sessions of 2–4, in small/medium complete-scene levels. | Bounds replication, hosting, content, and performance work; open-world streaming is deferred. |
| D-013 | Accepted | Agent-authored changes use one Issue, one dedicated branch, one pull request, and CI before merge. | Makes handoff state reviewable and prevents undocumented direct changes to `master`. |
| D-014 | Accepted | Commit-contained status and live workflow status are separate. | `docs/DEVELOPMENT_STATUS.md` describes its containing commit; GitHub Issues/Project describes activity after that commit. |
| D-015 | Accepted | The 15-second P0-T12 combined run is a smoke test, not a soak test. | P0-T13 / Issue #43 owns the 15-minute sustained run; P0-T14 / Issue #44 owns repeated lifecycle evidence. |
| D-016 | Accepted | Every declared Gradle subproject owns an explicit package root and exposes only its declared `.api` root as a cross-module contract; declared `.internal` roots are implementation details and must not be imported by other modules. | Makes module boundaries machine-checkable without designing future subsystem APIs early. A source-scanning architecture test validates the registry and rejects cross-module implementation imports, including a representative game-to-platform shortcut. |
| D-017 | Accepted | D-016 verification derives its complete subproject inventory from Gradle and parses main/test Java source with the JDK compiler-tree API; production sources must declare an owned package, while test-package ownership is intentionally excluded. | Prevents hard-coded module drift, missing-package bypasses, fully qualified implementation shortcuts, and overlapping-root misattribution without adding a parser dependency. This is source-level enforcement and does not claim bytecode, reflection-string, resource, or out-of-tree generated-source coverage. |
| D-018 | Accepted | P2-T01 / Issue #64 defines `EngineSubsystem` as an abstract `AutoCloseable` base with final guarded lifecycle methods and protected phase hooks. One instance is initialized, started, stopped, and closed once; unstarted or failed instances can be explicitly closed. Forward hook failures propagate unchanged and allow only cleanup; close is attempted at most once even on failure. Calls are externally serialized and reentrant transitions are rejected. | Makes single-subsystem ordering executable without prematurely implementing a dependency graph or coordinated rollback. Cleanup hooks tolerate partial setup and failed activation/stopping. A failed cleanup is not proof of resource release. This does not authorize native restartability claims or supersede P0-T14. |
| D-019 | Accepted | P2-T02 / Issue #72 adds an immutable, non-owning `SubsystemGraph` registration snapshot with deterministic depth-first dependency ordering. Construction rejects duplicate IDs/instances and missing prerequisites; resolution rejects cycles with a closed diagnostic path and returns only a complete unmodifiable order. Graph operations invoke no lifecycle methods. | A manual list cannot validate declared dependencies; graph-only resolution supplies that validation without a lifecycle manager, service locator, new dependency, or rollback. The caller resolves before initialization and retains D-018 ownership/cleanup duties. Iterative traversal avoids call-stack limits. P2-T03 retains coordinated rollback; native and phase-exit evidence is unchanged. |
| D-020 | Accepted | P2-T03 / Issue #73 adds stateless `SubsystemStartup.start(List<EngineSubsystem>)` coordination for an already resolved dependency-first order. Startup activates each subsystem with `initialize()` then `start()` before advancing. On initialize/start failure, it preserves the original throwable, closes the failing subsystem, then stops and closes previously started subsystems in reverse order; rollback failures are suppressed on the original failure. Successful startup retains caller ownership and normal shutdown responsibility. | P2-T02 produces validated ordering but intentionally performs no lifecycle work. Centralizing only partial-startup rollback prevents duplicated try/finally logic without introducing a general lifecycle manager, state accessor, restart contract, dependency injection framework, or normal-shutdown owner. The utility is synthetic Java lifecycle evidence only and does not establish native restartability, leak freedom, sustained stability, or the P2 phase exit. |
| D-021 | Accepted | P2-T04 / Issue #74 adds `EngineClock` as a small elapsed-nanosecond sampler. The default source is `System.nanoTime`; tests may inject a `LongSupplier`. Construction does not sample. The first successful sample returns zero, later samples return `current - previousAccepted`, zero elapsed is valid, and a negative signed difference is rejected without advancing the accepted baseline. Raw source values may be negative; ordinary `long` subtraction intentionally preserves forward intervals below `2^63` ns across signed wraparound. Source failures propagate unchanged and do not advance state. Calls are externally serialized. | Centralizes deterministic monotonic elapsed sampling without assigning fixed-step, pacing, catch-up, interpolation, wall-clock, frame-identity, or concurrency policy to the clock. Integer nanoseconds avoid premature floating-point rounding policy and provide the direct input needed by P2-T05 while keeping later timing tasks separately testable. |
| D-022 | Accepted | P2-T05 / Issue #75 adds `FixedStepAccumulator` as a stateful integer/rational conversion from non-negative elapsed nanoseconds to newly due whole simulation ticks at exactly 60 ticks per 1,000,000,000 ns. Fractional progress is retained exactly across calls; negative input is rejected without mutation; zero is valid; all non-negative `long` inputs, including `Long.MAX_VALUE`, are supported without intermediate overflow. The caller owns tick execution/counting and externally serializes calls. | Preserves cadence-independent fixed-step progress without rounding 1/60 second to an integer nanosecond duration or introducing floating-point drift. The accumulator deliberately excludes frame-gap clamping, catch-up limits, interpolation exposure, pacing, callbacks, lifecycle integration, and configurable tick rates, which remain later bounded tasks. |
| D-023 | Accepted | P2-T06 / Issue #76 adds `FixedStepCatchUpPolicy` above `FixedStepAccumulator`. The default policy clamps one incoming elapsed duration to 250,000,000 ns and exposes at most 5 whole simulation steps from one update. Positive explicit limits are supported. Elapsed time above the frame-gap bound and whole due ticks above the step cap are deliberately discarded; accepted fractional sub-tick progress remains owned by the accumulator. Negative elapsed and non-positive limits are rejected before accumulator mutation. | Prevents a delayed frame from creating an unbounded catch-up burst or persistent backlog while keeping D-022's exact 60 Hz arithmetic separate from recovery policy. The caller still owns the clock, simulation execution, tick numbering, pacing, lifecycle, and serialization. P2-T07 retains interpolation exposure; P2-T08/P2-T09 retain configuration work; the ten-minute P2 integration gate remains separate. |
| D-024 | Accepted | P2-T07 / Issue #77 adds `FixedStepAccumulator.interpolationAlpha()` as a read-only normalized view of the accumulator's exact retained fractional progress. The method returns `scaledRemainder / 1_000_000_000.0` in `[0,1)`, does not mutate or consume progress, and does not alter `advance(long)` or the fixed 60 Hz simulation contract. Floating point is presentation-only for interpolation alpha; simulation progression, accumulation, frame-gap clamping, and catch-up policy remain integer/rational. | Rendering needs sub-tick presentation progress without receiving a variable simulation delta or leaking the accumulator's internal remainder representation. Alpha reflects only accepted retained fractional progress; time/whole ticks discarded by D-023 do not become alpha or future backlog. The caller still owns simulation execution, previous/current render states, interpolation itself, and serialization. |
| D-025 | Accepted | P2-T08 / Issue #78 adds a fixed startup configuration schema in `engine-core` that validates one already-resolved raw map into immutable typed values. The schema supplies defaults, preserves opaque source locations in diagnostics, rejects unknown keys, aggregates user configuration errors in deterministic input order, and invokes no subsystem lifecycle. Initial keys are fullscreen width/height plus `simulation.tickRate`, which accepts only the currently locked value 60. | Central startup validation keeps parsing/bounds failures out of subsystem initialization without prematurely building a generic configuration framework. P2-T09 retains source layering/precedence. The 60 Hz timing contract remains unchanged rather than becoming configurable as a side effect of adding a key. |
| D-026 | Accepted | P2-T09 / Issue #79 composes startup configuration in fixed precedence `EngineConfigSchema` defaults < optional game UTF-8 key/value file < optional user UTF-8 key/value file < already-parsed command-line overrides, then validates the final effective raw map exactly once through D-025. File entries preserve normalized `path:line` sources; missing files are absent layers; malformed lines, blank keys, and duplicate keys within one file fail before schema validation; unreadable existing paths propagate `IOException`. | Separates deterministic source layering from typed validation without adding environment-variable policy, raw argv parsing, hot reload, a generic provider framework, or another dependency. Validating only after merge means an invalid lower-precedence value does not block startup when a valid higher-precedence value replaces it, while winning invalid values retain accurate source diagnostics. |
| D-027 | Accepted | P2-T10 / Issue #80 adds `NativeResourceRegistry` in `engine-core` for explicit diagnostic tracking of native ownership. A live identity is normalized resource type plus opaque nonzero `long` handle; registration captures the allocation call site and returns the supported `AutoCloseable` registration. The closer runs synchronously on the caller thread, successful close unregisters exactly once, closer failure is terminal and remains tracked as `CLOSE_FAILED`, and `assertNoOpenResources()` reports tracked resources without force-closing or mutating them. | Later native wrappers need deterministic ownership diagnostics without relying on GC or hiding ownership bugs through automatic cleanup. External serialization preserves native thread-affinity policy. Synthetic registry tests do not prove native leak freedom, sustained stability, or restartability; P0-T13/P0-T14 and the ten-minute P2 integration gate remain separate evidence. |
| D-028 | Accepted | P2-T12 / Issue #82 adds `EngineLogger` in `engine-core` as one synchronous structured logging boundary with immutable events, automatically captured timestamp and caller-thread identity, optional validated frame/tick/subsystem/connection/entity context, caller-owned sink, explicit flush, serialized sink callbacks, and unchanged propagation of sink `RuntimeException`/`Error` failures. No logging framework, persisted format, async queue, background worker, global singleton, or automatic fatal termination is selected. | Later subsystem/network/world diagnostics need stable field access without parsing free-form text, and P2-T13 needs a bounded flushable logging seam before orderly fatal termination. Keeping the sink caller-owned and format-neutral avoids prematurely selecting routing/persistence/lifecycle policy; `FATAL` remains only a severity label until P2-T13. |
| D-029 | Accepted | P2-T13 / Issue #83 adds one-shot synchronous `FatalTermination` orchestration in `engine-core`: after full input validation it emits a structured `FATAL` event, attempts reverse `stop()` then `close()` for every supplied successfully started subsystem, verifies the existing non-force-closing native-resource registry, best-effort reports captured failures as structured `ERROR` events, flushes the caller-owned logger once, and only then invokes process termination with exit status `1`. The sequence stays on the caller's lifecycle/native-affinity thread; logging/cleanup/verification/flush failures are contained so they cannot skip later cleanup or termination, and reentrant/concurrent/later calls on the same coordinator are rejected. | Fatal failures need diagnostics and cleanup before process exit, while immediate `System.exit` is simpler but can lose logs and owned resources. Reusing D-018, D-027, and D-028 avoids a new lifecycle manager or force-close registry. P2-T13 selects no background shutdown thread, global fatal singleton, persisted log format, module edge, dependency, or composition-root wiring. Synthetic and child-JVM tests prove Java orchestration only; native feasibility gates and the ten-minute Phase 2 integration gate remain separate. |
| D-030 | Accepted | Issue #135 reduces the Phase 2 integrated headless-loop exit-gate duration from the original ten minutes to 60 continuous seconds. Only the duration changes: the gate still requires integrated fixed 60 Hz simulation ticks, bounded catch-up, orderly lifecycle shutdown, and verified `NativeResourceRegistry` cleanup. | Phase 2 uses this as an integration-correctness gate rather than a soak test. Sustained native stability remains P0-T13's separate 15-minute evidence requirement and repeated native lifecycle remains P0-T14; neither is weakened or replaced by the shorter Phase 2 gate. |
| D-031 | Accepted | P3-T01 / Issue #84 makes `engine-platform-lwjgl` own the first production GLFW/OpenGL platform lifecycle through one public `GlfwWindow` D-018 subsystem. One window lifetime explicitly owns one GLFW initialization session, one window/OpenGL context, one task-owned GLFW error callback, thread-local OpenGL capabilities while started, and one caller-registry registration for the window handle. It requests explicit OpenGL 4.6 Core hints, verifies actual 4.6 support, logs actual version/renderer through D-028, keeps native lifecycle hooks on the initializing thread, and exposes no raw GLFW/LWJGL handle or type. | Phase 3 needs a production ownership boundary rather than reusing the experimental Phase 0 executable. Keeping the public API to one lifecycle object prevents premature event/input/renderer coupling. This decision selects no multi-window manager, shared contexts, render loop, input/event contract, GL debug callback, or native restartability claim; P0-T13/P0-T14 remain separate evidence gates. |
| D-032 | Accepted | P3-T02 / Issue #85 extends the D-031 production window boundary with a renderer-neutral `WindowSizeListener` and owner-thread `GlfwWindow.pollEvents()`. Logical window dimensions and framebuffer pixel dimensions are captured through separate GLFW channels, queried independently for initial state, coalesced to the latest value per channel during one poll, and delivered after native polling returns in logical-then-framebuffer order. Zero framebuffer axes are valid minimized states; negative platform dimensions fail before public delivery. Native size callbacks stage values only and are explicitly released during stop/start-failure cleanup. | High-DPI rendering needs pixel dimensions without treating GLFW screen coordinates as pixels. A single size value or exposing raw GLFW callbacks/handles would be simpler but would either lose DPI information or leak the native adapter into consumers. Keeping delivery synchronous on the existing D-031 owner thread avoids an asynchronous dispatcher and callback reentrancy while preserving renderer neutrality. This adds no renderer, buffer-swap, fullscreen, focus/input, content-scale callback, raw-handle, multi-window, or restartability contract; P0-T13/P0-T14 remain independent evidence gates. |
| D-033 | Accepted | P3-T03 / Issue #86 extends `GlfwWindow` with in-place owner-thread transitions among `WINDOWED`, `BORDERLESS_FULLSCREEN`, and `EXCLUSIVE_FULLSCREEN` while STARTED. The same GLFW window/OpenGL context is retained. Windowed geometry is captured before the first fullscreen transition and restored on return. Borderless uses an undecorated non-monitor-owned window sized to the primary monitor's current video mode; exclusive attaches the same window to the primary monitor using that current mode and refresh. Transition failures preserve the original throwable and make one best-effort rollback attempt. | Runtime mode switching is required without recreating the context or leaking GLFW handles into callers. Primary-monitor/current-mode policy keeps the feature bounded and predictable. No monitor-selection API, custom display-mode/refresh selector, renderer integration, multi-window policy, input/focus behavior, or native restartability claim is introduced; D-031/D-032 ownership and event delivery remain in force and P0-T13/P0-T14 remain independent evidence gates. |
| D-034 | Accepted | P3-T04 / Issue #87 extends `GlfwWindow` with owner-thread `setCursorCaptured(boolean)` and explicitly owned focus/key/mouse-button callbacks. The platform boundary tracks bounded pre-snapshot held key/button state only for safety. Focus loss clears that held state before forcing effective cursor capture to `GLFW_CURSOR_NORMAL`; focus regain never automatically recaptures and requires an explicit later capture request. Failures raised while releasing capture inside a native focus callback are staged and propagated once from the owning `pollEvents()` call rather than crossing the native callback boundary. | Windows may stop delivering release events while a game is unfocused, so relying on key-up/mouse-up callbacks alone can leave stale held state. Keeping this safety state inside `GlfwWindow` solves the bounded P3-T04 requirement without prematurely introducing P3-T06 `InputSnapshot`, actions, raw mouse, controller policy, game pause/menu behavior, or a public hardware-state API. Callback ownership remains explicit under D-031, and P0-T13/P0-T14 remain independent evidence gates. |
| D-035 | Accepted | P3-T05 / Issue #88 makes `GlfwWindow` own relative mouse-motion acquisition while cursor capture is effectively active. It installs and owns a cursor-position callback, uses `GLFW_RAW_MOUSE_MOTION` when GLFW reports support, otherwise derives signed deltas from successive disabled-cursor positions, and keeps the accumulated motion/baseline internal until P3-T06. Capture, focus-loss, stop/close, and failed-start transitions clear pending deltas and invalidate the baseline; focus regain never re-enables capture or raw mode without an explicit later `setCursorCaptured(true)`. | First-person input needs relative motion that is independent of absolute screen position without coupling future gameplay/input code to GLFW. Raw mode provides unaccelerated motion when supported; the fallback preserves relative screen-bound independence but does not claim to bypass OS pointer acceleration. Keeping motion internal avoids freezing P3-T06 `InputSnapshot` semantics early. Native callback ownership/failure staging follows D-034, and P0-T13/P0-T14 remain independent evidence gates. |
| D-036 | Accepted | P3-T06 / Issue #89 makes `GlfwWindow` the producer of caller-triggered immutable renderer-frame `InputSnapshot` values. Snapshot capture is STARTED/owner-thread only, performs no GLFW poll, exposes only engine-defined `InputKey`/`InputMouseButton` vocabulary, consumes retained raw hardware press/release edges and accumulated relative mouse motion, and preserves held levels plus the D-035 motion baseline. Focus loss converts currently held supported inputs into pending release edges, discards stale pending presses, and retains D-034/D-035 capture/motion safety. | Multiple consumers need one stable per-frame hardware view without direct GLFW access or inconsistent ad-hoc polling. Keeping `InputSnapshot` in `engine-platform-lwjgl` adds no module edge and preserves GLFW/LWJGL behind the platform boundary. The snapshot is a client hardware view, not a headless/replay format: P3-T07/P3-T08 retain action semantics and P3-T09 retains tick-aligned `PlayerInputCommand`/replay portability, so `game-server` remains independent of the platform module. |
| D-037 | Accepted | P3-T07 / Issue #90 defines gameplay action bindings as immutable, device-neutral metadata loaded atomically from strict versioned JSON schema v1 inside `engine-platform-lwjgl`. The public API defines exactly eleven Phase 3 `InputAction` values, classifies MOVE/LOOK as `VECTOR2` and the remaining actions as `DIGITAL`, and uses immutable `InputBinding` descriptors over engine-owned key/button vocabulary plus relative mouse X/Y controls. Jackson 2.21.2 is an implementation-only parser dependency; unknown/duplicate schema content and invalid bindings fail before a binding set is returned. | Data-driven remapping requires a stable metadata/schema boundary without hard-coded gameplay switches. Keeping JSON parsing and binding metadata separate from runtime evaluation prevents T07 from stealing P3-T08 transition/aggregation semantics or P3-T09 tick/replay command scope. No project/module edge is added, Jackson is absent from public signatures, and `game-server` remains independent of the platform module. |
| D-038 | Accepted | P3-T08 / Issue #91 evaluates one P3-T06 immutable hardware `InputSnapshot` against one P3-T07 immutable `InputActionBindings` set through a caller-owned stateful renderer-frame `InputActionEvaluator`. Binding values add deterministically per component without clamp/normalization; action pressed/held/released transitions derive from previous successful aggregate activity, while a complete same-binding key/button press+release retained by P3-T06 is preserved as a one-frame action tap. Successful evaluations require strictly increasing source frame IDs and commit evaluator state atomically only after all action values are finite/valid. | A stateless or hard-coded action layer would either duplicate binding logic in gameplay or misreport overlapping-binding transitions. Keeping the evaluator stateful but renderer-frame scoped produces one stable action contract without pulling P3-T09 tick/replay commands forward. Cross-binding press/release evidence is not treated as an ordered tap because `InputSnapshot` contains no ordering between different controls. No dependency/module/schema change is added; P3-T09 remains the device-neutral tick/replay boundary and `game-server` stays independent of the platform module. |
| D-039 | Accepted | P3-T09 / Issue #92 defines immutable device-neutral `PlayerInputCommand` in `engine-core` as the simulation-tick input/replay boundary and keeps renderer-frame-to-tick accumulation in caller-owned `PlayerInputCommandSampler` inside `engine-platform-lwjgl`. Pending LOOK delta and digital pressed/released edges accumulate across zero-tick renderer frames and are consumed exactly once by the next emitted command; latest MOVE and digital scalar/held state repeat across later ticks until a newer frame arrives. `PlayerInputCommandCodec` is an explicit fixed-size ByteBuffer replay/storage codec v1 using magic `SPIC`, big-endian fields, exactly nine digital actions, and an exact 126-byte layout. | Headless/replay consumers need a stable command that does not depend on GLFW-facing types. Keeping the command/codec in `engine-core` preserves the existing headless-server boundary while the platform module adapts renderer-frame action snapshots without adding a project edge. The codec is not declared a production network packet layout; Java serialization remains forbidden. Strict frame/tick ordering and atomic rejection of invalid/non-finite input prevent partial sampler state advancement. P3-T10 retains sensitivity/inversion/dead-zone/controller response settings. |
| D-040 | Accepted | P3-T10 / Issue #93 defines immutable `InputResponseSettings` in `engine-core` as the deterministic device-response math contract. Mouse X/Y shaping is applied by `InputActionEvaluator` before existing binding scale and additive aggregation; neutral defaults preserve prior behavior. Runtime settings replacement affects future successful evaluator frames only and does not reset frame identity or transition history. Controller response is currently an axis-local scalar mapping with explicit dead-zone renormalization and curve exponent; it does not imply a production controller discovery, polling, vocabulary, callback, or binding API. | Response policy must be stable before renderer-frame actions become tick commands, otherwise camera/gameplay callers would duplicate or reorder sensitivity/inversion logic and replays could depend on presentation code. Keeping pure response math in core and the hardware-to-action application point in the existing platform evaluator adds no project edge, leaves P3-T09 `PlayerInputCommand`/codec/sampler unchanged, and deliberately defers controller capture/radial-stick policy until an executable task requires them. |
| D-041 | Accepted | P4-T01 / Issue #94 fixes Sherko Engine world space as right-handed, with +X right, +Y up, -Z forward, meters for linear world quantities, radians for internal angular quantities, positive rotation by the right-hand rule, and dimensionless transform scale. External libraries/formats with different axes or units convert explicitly at their adapter/import/export boundaries rather than redefining engine world space. | Later transform, camera, renderer, physics, audio, asset, and network-spatial work needs one shared basis before code starts depending on implicit library defaults. This decision does not select projection/NDC depth policy, reversed-Z, FOV/near/far rules, Euler storage/order, quaternion canonical sign, external-format/Jolt/OpenAL conversion details, or network quantization; those remain separate executable tasks. The normative contract is `docs/SPATIAL_CONVENTIONS.md`. |
| D-042 | Accepted | P4-T03 / Issue #96 makes JOML the public math type family for `engine-core` spatial APIs beginning with `Transform`. Local transform composition is `T * R * S`; world composition is `parentWorld * local`. Caller-owned JOML inputs and destinations are copied and never retained as internal mutable state. World matrices are cached and lazily validated against the parent world revision; zero/negative scale are allowed for forward composition. | A real transform API now requires reusable vector/quaternion/matrix values, so hiding the already-selected JOML library behind duplicate wrappers would add conversion and allocation pressure without value. The revision dependency keeps cached children correct before P4-T05 descendant propagation. Cyclic parent graphs remain unsupported until P4-T04 defines explicit rejection; inverse/decomposition policy remains future work. |
| D-043 | Accepted | P4-T04 / Issue #97 makes `Transform.setParent(...)` validate acyclicity synchronously before hierarchy mutation. A different non-null proposed parent is walked through its existing ancestor chain by identity; if that chain reaches the transform being reparented, self-parenting and indirect descendant cycles are rejected atomically with `IllegalArgumentException` and the stable message `parent assignment would create a transform cycle`. | Parent links must stay acyclic so cached world evaluation never discovers structural invalidity recursively after the hierarchy has already been mutated. The O(depth), allocation-free ancestor walk is sufficient because `setParent(...)` is the only public parent-mutation API. Same-parent no-op, detach, unrelated-parent reparenting, and legal reparenting to an existing ancestor remain valid. This adds no public child collection or hierarchy manager. |
| D-044 | Accepted | P4-T06 / Issue #99 defines immutable public `engine-core` world-geometry primitives `Ray3f`, `Plane3f`, `Sphere3f`, `Aabb3f`, and `Frustum3f`. JOML inputs are copied; ray directions and plane equations are normalized; contact/containment is boundary-inclusive with exact production comparisons and no hidden epsilon; ordinary ray misses return `Float.NaN`; frusta consume six inward-facing planes directly. | Spatial queries need one convention-consistent tested boundary instead of duplicated call-site formulas. The API deliberately stays independent of projection/NDC/depth policy, screen-to-world unprojection, renderer culling, physics adapters, serialization, and broad-phase structures so P4-T07/P4-T08 and later systems can consume these primitives without those policies being selected implicitly. |
| D-045 | Accepted | P4-T08 / Issue #101 defines `CameraMatrices` as the public `engine-core` camera-matrix construction boundary. View space is right-handed with camera forward mapped to `-Z`; perspective uses vertical FOV in radians, finite positive aspect and near plane with `far > near`, OpenGL NDC depth `[-1,+1]`, and conventional finite non-reversed depth. Methods replace caller-owned JOML destination matrices and retain no caller state. | P4-T07 screen-to-world rays and later rendering need one explicit projection/view contract instead of relying on library defaults. The convention preserves D-041 world space, matches the selected OpenGL renderer baseline without importing renderer/native code into `engine-core`, and deliberately leaves screen origin/pixel-center/viewport mapping and unprojection ray-origin policy to P4-T07. |
| D-046 | Accepted | P4-T07 / Issue #100 defines `ScreenRays.worldRay(...)` as the public `engine-core` screen-to-world boundary: viewport/screen origin is top-left, X increases right and Y down, continuous screen samples share the viewport coordinate domain, raster pixel centers are `index + 0.5`, closed viewport edges map to NDC `±1`, D-045 near/far clip depths are `-1/+1`, world unprojection uses inverse `projection * view` plus homogeneous division, and the returned normalized `Ray3f` starts on the unprojected near plane and points toward the corresponding far point. | Picking needs one explicit conversion instead of duplicated caller formulas that can flip Y, use the wrong depth range or inverse order, or disagree about camera-versus-near-plane ray origin. The API deliberately performs no GLFW logical/framebuffer scaling, renderer integration, physics query, camera-component ownership, cached projector policy, or orthographic/frustum extraction work; callers must supply sample and viewport values in the same coordinate domain. |
| D-047 | Accepted | P4-T09 / Issue #102 defines public `TransformQuantization` value semantics in `engine-core`: position is quantized independently per axis at exactly `1/64 m` into full signed 16-bit codes over `[-512.0, 511.984375] m` with maximum per-axis round-trip error `1/128 m`; normalized orientation uses deterministic smallest-three quaternion quantization, lowest-index largest-component tie breaking, and sign canonicalization so `q`/`-q` encode identically. Stored quaternion components use `[-32767,+32767]`, reserve `-32768` as malformed, and valid encode/decode angular error is bounded by `0.0002 rad`. | Later replication/storage design needs a compact bounded transform-value representation without prematurely freezing a transport packet. D-047 defines reusable values only: no byte order, packet layout/version, entity/tick association, authority, delta compression, transport, origin scheme, or scale quantization. Callers choose whether values represent local/world/origin-relative transforms under their own later contract while preserving D-041 units/basis. |
| D-048 | Accepted | P5-T01 / Issue #183 makes `GlfwWindow` own optional OpenGL debug-context diagnostics through public `OpenGlDebugMode`. Existing constructors default to `DISABLED`; `FAIL_ON_HIGH_SEVERITY` requests a GLFW debug context, verifies the context debug flag after capabilities creation, installs one context-bound LWJGL debug callback, normalizes and logs source/type/severity/message through `EngineLogger`, stages callback/logging failures instead of throwing across the native callback boundary, and surfaces high-severity failures once from owner-thread `pollEvents()`. Stop/start-failure/close release the callback before context detachment/capability clearing. | The platform boundary already owns the OpenGL context lifetime, so it is the narrowest correct owner of the context-bound callback. An implicit JVM/system-property switch was rejected because it creates hidden process-global behavior and weakens release safety; throwing directly from a native callback was rejected because Java failures must not cross that callback boundary. This adds no renderer resource/draw API, raw native handle, dependency, or module edge. |

| D-049 | Accepted | P5-T02 / Issue #184 establishes one engine-owned `OpenGlThreadGuard` per `GlfwWindow`. The guard is bound exactly once by `initialize()` to the same thread that owns the GLFW/OpenGL lifecycle, rejects use before binding, rejects non-owner callers with `IllegalStateException`, and remains the single affinity rule consumed by `GlfwWindow` lifecycle checks and future GPU-facing wrappers. `GlfwWindow.openGlThreadGuard()` exposes the stable non-owning guard without exposing the raw owner `Thread`, GLFW/OpenGL context, handle, queue, or executor. | Future OpenGL wrappers need one shared ownership authority rather than each capturing its own thread identity. A guard centralizes the rule and guarantees worker-thread rejection before native entry while avoiding premature render workers, command queues, or P5-T03 resource wrappers. Stop/close do not transfer ownership. |

| D-050 | Accepted | P5-T03 / Issue #185 keeps the first OpenGL resource wrappers internal to `engine-render-opengl`: buffer, vertex array, texture, sampler, shader, program, and framebuffer. Every wrapper consumes D-049's `OpenGlThreadGuard`, registers successful native ownership with `NativeResourceRegistry`, provides idempotent wrapper-level close with native deletion attempted at most once, and performs bounded partial-failure cleanup. Shader/program wrappers include only compile/link lifecycle needed to prove ownership failure handling. `engine-render-opengl` directly reuses the already-selected LWJGL 3.4.3 OpenGL dependency. | Internal wrappers establish deterministic GPU ownership without prematurely freezing a public renderer-resource API or introducing a general resource manager/cache. Wrong-thread operations fail before native/registry mutation, registry-registration failure cleans the just-created handle, shader/program setup failures clean only newly-owned native objects, and no draw/upload/material/asset policy is selected. |

| D-051 | Accepted | P5-T04 / Issue #186 uses one internal fixed-slot dynamic-buffer ring for the first room workload. One P5-T03 owned buffer is allocated once with explicit total capacity `slotCount * slotCapacityBytes`; uploads use bounded sub-data writes, advance in strict round-robin order, and return internal immutable slice tokens. Submitted slots receive one registered `GLsync` fence; a slot may be reused only after a non-blocking fence check reports signaled. Timeout/wait failure and uploaded-but-unsubmitted wrap reject before buffer mutation or cursor advance. | This is the smallest synchronization strategy that prevents overwriting GPU-visible dynamic data while keeping capacity explicit. Unsynchronized single-region sub-data is unsafe, whole-store orphaning does not model the required bounded wrap/reuse contract, and persistent mapping/general streaming allocation are deferred until representative frame data exists. No performance superiority claim is made. |

| D-052 | Accepted | P5-T05 / Issue #187 validates committed runtime GLSL offline with the repository-locked LWJGL 3.4.3 Shaderc binding and verifies the same committed sources again through the production OpenGL 4.6 runtime compile/link path. The renderer module keeps Shaderc on test/build-validation classpaths only; no machine-global shader compiler is required. Runtime shader/program failures preserve internal source/program identity plus complete backend diagnostics while retaining P5-T03 cleanup ownership. | Build-time validation catches committed shader breakage before normal runtime entry without introducing a machine-global tool dependency. Reusing the scope-selected LWJGL family/version avoids a new dependency family. Runtime verification remains necessary because driver compile/link behavior is still authoritative for actual OpenGL execution. No material, variant, reflection, hot-reload, asset, or public shader API is selected. |

| D-053 | Accepted | P5-T06 / Issue #188 defines two internal std140 renderer uniform blocks with fixed ABI: `CameraBlock` at binding 0 is exactly 128 bytes with `view` at offset 0 and `projection` at offset 64; `PerFrameBlock` at binding 1 is exactly 16 bytes with `framebufferSizeAndInverse = (width, height, 1/width, 1/height)` at offset 0. CPU packing preserves caller-supplied D-041/D-045 matrices in GLSL/OpenGL column-major order without transposition. Runtime reflection verifies exact block presence, size, and binding against the linked committed P5 shader program. | The first indexed-mesh path needs one deterministic camera/per-frame ABI before any draw code exists. Keeping only view/projection and framebuffer dimensions avoids prematurely freezing camera position, time, lighting, material, jitter, or gameplay fields. Explicit reflection catches CPU/GLSL drift while preserving the accepted right-handed, forward-on--Z, finite non-reversed OpenGL depth convention. No public renderer API, UBO allocator, descriptor system, world camera ownership, or material block system is selected. |

| D-054 | Accepted | P5-T07 / Issue #189 introduces the first public production renderer composition as bounded `OpenGlRenderer`: it owns one internal indexed position-only triangle, its VAO/VBO/EBO, P5-T06 camera/per-frame UBOs, committed shaders/program, and explicit draw state. Every render uses full-framebuffer viewport, depth test `GL_LESS`, back-face culling `GL_BACK`, front face `GL_CCW`, and exactly one indexed `GL_TRIANGLES` draw of three unsigned-int indices. `GlfwWindow.present()` is the owner-thread/start-state buffer-swap boundary and exposes no native window handle. The persistent sandbox composes these public APIs and closes the renderer before the context/window. | The first visible production draw requires a cross-module renderer boundary and window-owned presentation without exposing native implementation handles. A fixed known mesh is sufficient to establish resource/state/uniform/presentation composition while arbitrary meshes, assets, materials, lighting, sRGB policy, world/ECS submission, batching, culling/sorting, and renderer job systems remain future tasks. Retained native evidence uses pipeline primitive-count plus framebuffer PNG capture as the authorized equivalent capture for this bounded task. |

| D-055 | Accepted | P5-T07A / Issue #213 makes `engine-render-opengl` publish the dependencies required by `OpenGlRenderer` signatures as Gradle API metadata and separates its normal compile artifact from its runtime artifact. `apiElements` contains only `com.samo.engine.render.api` classes and removes the Java plugin's full-main `classes` secondary artifact, while `runtimeElements` retains the complete renderer implementation and shader resources. `engine-core` and `engine-platform-lwjgl` are API dependencies because their public types occur in `OpenGlRenderer`; JOML remains transitively exposed through the existing `engine-core` API. | P5-T07 established `OpenGlRenderer` as the only supported renderer entry point, but its initial `implementation` dependency metadata could leave consumers without signature types and the default compile artifact exposed a public class under the declared `.internal` root. A distinct compile artifact preserves the accepted API/internal package contract without JPMS, a new module edge, source relocation, or renderer redesign. LWJGL and unrelated renderer dependencies remain implementation-only, and runtime ownership/thread-affinity behavior is unchanged. |
| D-056 | Accepted | P5-T08 / Issue #190 establishes the first explicit renderer color-space path. Display-referred color textures use `GL_SRGB8_ALPHA8` so sampling decodes stored sRGB bytes to linear shader values; linear-data textures use `GL_RGBA8` and receive no sRGB decode. Production `GlfwWindow` requests an sRGB-capable default framebuffer, but the renderer queries the actual default-back-buffer color encoding because GLFW framebuffer hints are not guaranteed. If the default buffer is `GL_SRGB`, `OpenGlRenderer.render(...)` enables `GL_FRAMEBUFFER_SRGB` and hardware performs the single presentation encode. If it is `GL_LINEAR`, framebuffer sRGB remains disabled and the fixed fragment shader performs exactly one IEC sRGB encode; the clear color is encoded consistently for that path. The fixed P5-T07 validation triangle samples one renderer-owned 1x1 neutral-gray sRGB texture through an internal sampler. | The first textured/color-correct renderer path needs one explicit decode/encode contract before materials or asset loading exist, but native evidence showed that requesting an sRGB-capable default framebuffer does not guarantee one on every Windows OpenGL configuration. Runtime negotiation preserves one presentation encode without exposing a public presentation API or pulling P5-T15 forward. A fixed internal texture and bounded linear-framebuffer fallback keep P5-T09 materials, P6 asset/cooker/mip work, HDR, tonemapping, fog, and post-processing out of this task. |

| D-057 | Accepted | P5-T09 / Issue #191 defines the first renderer material value as package-internal `RenderMaterialDescriptor` in `engine-render-opengl`. Each immutable material carries one explicit shader variant, immutable non-owning texture/sampler bindings, bounded linear scalar color multipliers, blend mode, depth mode, and cull mode. Native ownership remains in existing P5-T03 wrappers. The first fixed reference scene reuses the same indexed mesh, texture, sampler, and linked program twice with two material values; material policy selects fixed blend/depth/cull state and linear color multiplication, while P5-T08 remains responsible for the single presentation encode. The public `OpenGlRenderer` signature remains unchanged. | P5-T09 needs data-driven renderer state now, but P6 has not yet established stable cooked asset IDs/resource handles. A public material API would therefore freeze resource references prematurely. Internal material values satisfy the current renderer-foundation need while deferring public resource/material contracts, asset formats, submission packets, lighting, and gameplay/world coupling to their planned tasks. |

| D-058 | Accepted | P5-T10 / Issue #192 defines public `RenderFramePacket` as the first immutable per-frame renderer submission boundary. The packet snapshots finite view/projection matrices and positive framebuffer dimensions at construction, retains no mutable world/gameplay object, owns no native resource, exposes matrix state only by copying into caller-owned destinations, and is consumed synchronously without retention. `OpenGlRenderer.render(RenderFramePacket)` is the canonical new boundary; the prior matrix/size overload remains as a compatibility convenience that constructs an equivalent packet. Current P5 mesh/material/native resources remain renderer-owned internals. | P5-T10 must establish a world-facing extraction boundary before later culling/sorting work, but accepted P5-T09 deliberately deferred stable resource identities to P6. A frame snapshot containing only data that can be expressed honestly today creates the immutability/lifetime boundary without inventing temporary public mesh/material/asset/native handles. Draw-item resource identity, culling inputs, sorting keys, async queues, and ECS/world extraction remain later contracts. |

| D-059 | Accepted | P5-T11 / Issue #193 performs CPU visibility testing before the current fixed renderer draw submissions by extracting a world-space frustum from the accepted D-041/D-045 `projection * view` convention, constructing P4 `Plane3f`/`Frustum3f`, and delegating each decision to inclusive `Frustum3f.intersects(Aabb3f)`. The fixed reference mesh owns one immutable world AABB derived from its committed vertices. Public `RenderCullingCounters` report tested, visible, culled, and submitted-draw counts from the latest successful frame; failed renders do not publish partial counters. | The renderer needs deterministic off-camera rejection before P5-T12 ordering, but P4 already owns geometry/contact semantics and P6/P7 do not yet provide general resource/world submission. Reusing P4 geometry over the fixed P5 candidate set proves the culling boundary without duplicating geometry rules or inventing world/ECS, broad-phase, resource-ID, sorting, occlusion, or GPU-culling contracts. |

| D-060 | Accepted | P5-T12 / Issue #194 sorts only the renderer-internal visible submission list created after D-059 culling. Immutable `DrawSubmission` values carry stable logical program/material/mesh keys, camera-space depth, original sequence, current internal material reference, and fixed viewport data. Opaque submissions sort ascending by program/material/mesh/sequence. Transparent submissions sort back-to-front by camera-space depth, then by the same logical keys and sequence for deterministic ties. Sorting copies the source list and does not mutate submissions/materials/frame packets. | The current two-material scene already contains opaque and alpha-blended candidates, so deterministic ordering can be established without inventing public P6 resource identities or a batching/render-graph system. Raw OpenGL handles are rejected as ordering keys because their values are driver/process artifacts rather than stable renderer identities. |

| D-061 | Accepted | P5-T13 / Issue #195 defines the first lighting path as one package-internal immutable renderer-owned `DirectionalLight`. Its stored direction is the normalized D-041 world-space direction in which light rays travel; diffuse response is `max(dot(normal, -direction), 0)`. Linear RGB and the bounded SDR intensity scalar are finite values in `[0,1]`. The fixed P5 reference mesh adds renderer-internal +Z normals, and the fragment path multiplies decoded linear texture/material color by directional color, intensity, and Lambert diffuse before the existing D-056 single presentation encode. | The first room needs explicit lighting semantics without freezing a public light/world/resource contract before later phases own those boundaries. Keeping the one light internal preserves D-057/D-058 material/submission contracts, P5-T11 culling, P5-T12 ordering, and existing native ownership while deferring shadows, local lights, PBR/IBL, HDR/tonemapping, world/ECS light ownership, and P6 resource identity. |

| D-062 | Accepted | P5-T14 / Issue #196 introduces public immutable renderer-submission `RenderPointLight` and `RenderSpotLight` values plus an ordered immutable local-light list in `RenderFramePacket`. Local positions/ranges use D-041 world-space meters; spot direction is normalized D-041 ray-travel direction; color/intensity are linear bounded `[0,1]`; point range response is `(1-distance/range)^2` inside range and zero otherwise; spot response multiplies that by `smoothstep(cos(outer), cos(inner), dot(lightToSurface, spotDirection))`. One combined configured maximum in `[1,8]` truncates deterministically to the first N packet lights and emits one renderer WARN per overflowing render. A separate 528-byte std140 `LocalLightBlock` at binding 2 stores eight fixed-capacity entries without changing CameraBlock/PerFrameBlock ABI. Local contributions add to D-061 directional diffuse in linear space and bounded SDR illumination is clamped before material multiplication/presentation encode. | The locked v1 renderer is forward with limited dynamic lights, so a small explicit fixed ceiling establishes usable point/spot submission and overflow behavior without an unbounded SSBO, clustered/Forward+, world/ECS ownership, shadow resources, or P6 resource identities. Keeping local lights as immutable per-frame renderer data matches D-058 packet ownership, preserves the existing native/thread/resource boundaries, and gives the persistent sandbox a real public path without promoting later world/asset architecture. |

| D-063 | Accepted | P5-T15 / Issue #197 finalizes the bounded Phase 5 presentation policy as package-internal `SrgbPresentationMode` plus `SrgbTransfer`. The actual default back-buffer encoding is selected once at renderer creation: `GL_SRGB` uses shader-linear output plus `GL_FRAMEBUFFER_SRGB`; `GL_LINEAR` uses exactly one manual IEC 61966-2-1 fragment/clear encode with framebuffer sRGB disabled. CPU clear encoding uses the same explicit piecewise transfer thresholds/constants as the committed GLSL fallback. P5-T08 texture storage/decoding remains unchanged and P5-T13/P5-T14 shading stays linear through material and light accumulation. | P5-T08 already proved the mechanism, but its mode-specific behavior remained distributed across a raw boolean, shader-source injection, and clear code. P5-T15 freezes that mechanism as one internal policy and adds direct known-linear presentation evidence without creating public presentation settings, HDR/tonemapping, offscreen framebuffer architecture, render graphs, fog, bloom, exposure, or color grading. |

| D-064 | Accepted | P5-T16 / Issue #198 defines renderer-neutral per-frame debug diagnostics in `engine-core`: bounded immutable line/AABB/sphere/ray primitives, linear debug RGB, bounded label/value text counters, and a `DebugFrame` snapshot limited to 64 geometry primitives and 16 counters. `RenderFramePacket` carries that immutable snapshot. The OpenGL renderer expands geometry into one fixed-capacity world-space line buffer (AABB 12 edges; sphere three 16-segment great circles; finite ray length), draws it after the accepted scene with depth test on/depth write off/no blend/no cull/no lighting, and applies the accepted D-063 presentation encode. Text counters are not glyph-rendered; only the latest successful immutable counter list is published through `OpenGlRenderer.lastDebugTextCounters()`. | Keeping the vocabulary in `engine-core` lets later physics/network-capable lower modules produce diagnostics without depending on the renderer, while a bounded per-frame snapshot prevents retained debug-scene/lifetime ownership from appearing prematurely. Reusing the existing sandbox console for label/value counters avoids pulling font/UI/ImGui work forward. |

| D-065 | Accepted | P5-T17 / Issue #199 adds one package-internal first-person view-model validation layer separate from ordinary world submissions. The layer uses identity view plus an explicit 55° vertical FOV perspective at current framebuffer aspect, 0.01 m near and 10 m far, while caller-provided world view/projection remain exactly D-041/D-045. Ordering is world scene -> P5-T16 debug geometry -> depth-only reset -> view-model fixture. The view-model pass uses `GL_LESS`, depth writes enabled, blending/culling disabled, its own camera UBO/VAO/VBO/shaders/program, fixed linear color, and D-063 presentation encoding. The engine-owned six-vertex fixture is validation-only and no public view-model mesh/material/asset submission API is created. | Clearing only depth before a dedicated depth-tested layer proves held-object isolation without mutating world-camera semantics or introducing an FBO/render graph. Keeping the fixture internal avoids freezing a premature asset/resource contract before P6 and avoids pulling weapons/hands, animation/IK, third-person presentation, or HUD/UI into the rendering-foundation phase. |

| D-066 | Accepted | P5R-T07 / Issue #267 renames the supported public lifecycle coordination types `SubsystemStartup` -> `SubsystemStartupCoordinator` and `FatalTermination` -> `FatalTerminationCoordinator`. Method names, signatures apart from the declaring/constructor type names, lifecycle ordering, ownership, rollback, failure identity/suppression, fatal cleanup/reporting/flush ordering, one-shot behavior, and exit status remain unchanged. No compatibility aliases are retained. | The prior names read like values/events even though both types coordinate multi-step lifecycle sequences. T07 is the bounded lifecycle naming task, and the `Coordinator` suffix makes responsibility explicit without adding a lifecycle manager or changing D-018/D-020/D-029 semantics. The engine is pre-v1, so retaining deprecated aliases would add stale public surface without a compatibility requirement. D-020 and D-029 remain the historical behavior decisions and are not rewritten. |

| D-067 | Accepted | P6-T01 / Issue #362 defines public `AssetId` in `engine-assets` as an immutable 128-bit value represented by two `long` components. Identity is independent of source-file and metadata paths. New authoring identities may be generated opaquely; the canonical external text is lowercase UUID-style `8-4-4-4-12` hexadecimal and parse/format preserves all 128 bits exactly. The all-zero value is valid and has no sentinel meaning. P6-T01 introduces no metadata schema, cooker, manifest, runtime cache/handle, path hashing, or loading contract. | Phase 6 needs references that survive source-file relocation before metadata/cooking/runtime-resource layers are defined. Two fixed-width 64-bit halves mechanically establish the required 128-bit value without making source paths part of identity. A canonical textual form supports diagnostics and later metadata serialization without freezing P6-T02's metadata schema. |

| D-068 | Accepted | P6-T02 / Issue #365 defines strict UTF-8 source-asset metadata JSON schema version 1 with exactly `schemaVersion`, `assetId`, and `assetType`. `assetId` uses the accepted D-067/P6-T01 canonical `AssetId` text; `assetType` is exactly one of MESH, TEXTURE, MATERIAL, SKELETON, ANIMATION, AUDIO, PREFAB, or SCENE. Unknown fields, duplicate JSON fields, malformed/wrong-type values, unknown asset types, and noncanonical IDs fail atomically. Any schema version other than 1 fails with an upgrade-required diagnostic; no migration/fallback is performed. `engine-assets` reuses the repository-selected Jackson 2.21.2 implementation-only parser dependency, with no Jackson type in public signatures. | Phase 6 needs a stable persisted authoring metadata boundary before the cooker exists. A strict minimal common schema provides versioned identity/type input without inventing importer/cooker settings or deriving identity from file paths. Reusing the already-selected production JSON parser avoids a bespoke parser and keeps format semantics explicit. |

| D-069 | Accepted | P6-T03 / Issue #368 defines the first deterministic offline cooker convention. Source metadata sidecars are adjacent `<source>.asset.json` files discovered recursively without following symbolic links. A clean cook requires an absent non-overlapping output path and emits `manifest.json` schema v1 plus `assets/<AssetId>.bin`. Manifest entries are sorted by canonical `AssetId` and contain exactly `assetId`, `assetType`, normalized input-relative `sourcePath`, output-relative `cookedPath`, and positive `byteSize`. T03 payloads are temporary opaque byte-for-byte source copies; later bounded importer/cooked-format tasks may supersede payload content without changing D-067 identity. Pre-existing output is never overwritten/deleted, and newly created partial output is cleaned best-effort on failure. | Phase 6 needs a real cooker/output/manifest boundary before format import begins. Metadata-driven discovery preserves stable identity and avoids guessing by extension, while the opaque T03 payload proves the full command/discovery/cache/manifest path without pulling Assimp, decoding, coordinate conversion, or P6-T07's final binary schema forward. Refusing existing output prevents a mistaken CLI path from deleting user data. |

| D-070 | Accepted | P6-T04 / Issue #371 activates the already scope-locked LWJGL 3.4.3 Assimp binding inside `engine-assets` for offline MESH glTF import only. `.gltf` / `.glb` meshes are imported with no Assimp post-process flags; native `AIScene` data is copied immediately into Java-owned package-private arrays and every non-null scene is released with `aiReleaseImport` before returning. Assimp's glTF importer intrinsically compacts/remaps indexed vertices by first index use and converts glTF UV V to Assimp's lower-left UV convention; P6-T04 accepts those format-decode semantics but adds no handedness/axis/unit/winding conversion, second UV flip, node pre-transform, normal/tangent generation, or optimization. P6-T05 owns the exactly-once Assimp-import-basis-to-engine spatial coordinate/unit conversion. P6-T03 manifest schema/output paths remain unchanged, and MESH payload bytes remain temporary pass-through until P6-T07 defines the persisted mesh schema. | The roadmap explicitly selects Assimp for glTF mesh import and separately schedules spatial conversion and cooked binary layout. Using zero post-process flags makes the import boundary mechanically auditable. Explicitly documenting Assimp's built-in glTF vertex remapping and UV-origin normalization prevents those format-decode semantics from being mistaken for D-041 engine-space conversion; spatial coordinate/unit conversion remains isolated to P6-T05. Immediate Java copying plus unconditional scene release prevents native-pointer lifetime from escaping the offline importer. |

| D-071 | Accepted | P6-T05 / Issue #374 defines the exactly-once glTF/Assimp-import-basis to D-041 engine-space mesh conversion as `(x,y,z) -> (-x,y,-z)` for positions, normals, and tangent xyz, with linear scale exactly 1.0. The mapping is a 180-degree +Y rotation with determinant +1, so handedness, winding, and triangle indices remain unchanged. UV0 remains exactly the accepted P6-T04 Assimp import-basis value and its intrinsic glTF normalization is not repeated. A distinct package-private `ImportedMesh -> EngineMesh` converter is the mechanical exactly-once boundary; node/world transform baking is not part of this task. | glTF and D-041 are both right-handed and meter-based but define right/forward with opposite X/Z signs. Converting both X and Z matches the accepted axes without reflection, avoids hidden unit scaling, and prevents later renderer/runtime code from owning source-format spatial conversion. |

| D-072 | Accepted | P6-T06 / Issue #377 supersedes D-070 only for tangent generation: glTF MESH import now uses exactly `aiProcess_CalcTangentSpace` and no other Assimp post-process flag. A mesh requires tangent-space validation when its referenced Assimp material has `aiTextureType_NORMALS`; UV0 is the only supported normal-map UV channel in this slice. Required normal-map meshes fail before output creation when UV0 or normals are missing, when the normal texture selects another UV channel, or when tangent/bitangent output is unavailable. Imported tangent xyz is paired with a per-vertex handedness sign derived from normal/tangent/bitangent and preserved unchanged through D-071's determinant +1 engine-space conversion. No source metadata schema changes. | P6-T06 explicitly owns missing-tangent generation and required-UV rejection. Reusing Assimp's bounded tangent-space pass avoids a second custom generator, while material-driven validation avoids rejecting meshes that do not use tangent-space normal mapping. Preserving handedness now prevents mirrored-UV information loss before P6-T07 defines the persisted mesh stream. |

| D-073 | Accepted | P6-T07 / Issue #380 defines persisted MESH payload `SMES` schema v1. All multibyte fields are little-endian. The 20-byte file header is magic `SMES`, schemaVersion=1, positive meshCount, exact bodyByteLength, and CRC32C of the entire body. The decoder validates header/length/checksum before parsing records. The body is a sequence of 48-byte mesh headers plus strict UTF-8 name bytes, interleaved vertices, and int32 triangle indices. Attribute flags encode optional normals, tangent XYZS, and UV0; positions are mandatory. Each record stores exact D-041 engine-space AABB and decode recomputes it exactly. Unknown flags, malformed lengths, non-finite values, invalid tangent signs/indices, duplicate meshIndex, malformed UTF-8, bounds mismatch, truncation/trailing data, and checksum failure are rejected. P6-T07 replaces only the temporary MESH source-byte payload; manifest v1, AssetId-based paths, and non-MESH pass-through remain unchanged. | Phase 6 needs a deterministic source-format-independent mesh payload before runtime resource loading. A bounded little-endian interleaved schema plus JDK CRC32C provides explicit compatibility and corruption detection without Java serialization, native-order ambiguity, new dependencies, compression, public runtime APIs, or renderer/GPU integration. |

| D-074 | Accepted | P6-T08 / Issue #383 defines offline TEXTURE cooking for PNG/JPEG sources through the scope-selected LWJGL 3.4.3 stb binding. Supported `.png`, `.jpg`, and `.jpeg` sources are decoded only in the cooker to tightly packed, unflipped RGBA8, then converted into a complete deterministic mip chain where each next dimension is `max(1, previous/2)` and each output channel is the unsigned integer floor-average of the available 2x2 source texels. Persisted TEXTURE payloads use little-endian `STEX` schema v1 with fixed RGBA8 format, base dimensions, complete mip count, exact body length, ordered per-mip dimension/length records, and CRC32C over the full body. Decode validates the complete header/body/checksum/chain before returning package-private cooked texture values. Source metadata schema v1 and manifest schema v1 remain unchanged. P6-T08 intentionally assigns no sRGB-versus-linear semantic meaning and performs no gamma transform, normal-map renormalization, alpha premultiplication, compression, GPU upload, runtime resource loading, or material dependency work. | Phase 6 requires source image decoding to disappear from runtime inputs before resource loading exists. Reusing the already selected LWJGL family avoids a new dependency family, while an explicit deterministic RGBA8/mip binary gives later runtime code source-format-independent validated bytes. Deferring color-space/usage semantics avoids inventing metadata not authorized by the current schema and does not redefine renderer D-056. |

| D-075 | Accepted | P6-T09 / Issue #386 defines offline AUDIO cooking for case-insensitive `.ogg` Ogg Vorbis sources through the already selected LWJGL 3.4.3 `stb_vorbis` binding in `engine-assets`. The cooker reads the source into Java-owned bytes, copies them into explicitly freed native memory, opens the complete in-memory source through `stb_vorbis_open_memory`, obtains metadata through `stb_vorbis_get_info`, drains interleaved samples through `stb_vorbis_get_samples_short_interleaved` until EOF, requires the terminal decoder error to remain `VORBIS__no_error`, requires channels to be exactly mono or stereo and sample rate to be positive, and closes/frees all temporary decoder resources, then preserves the exact validated compressed Ogg Vorbis source bytes in persisted little-endian `SAUD` schema v1. `SAUD` stores magic, schema version, fixed Ogg Vorbis codec id, channels, sample rate, exact payload length, CRC32C, and the original compressed payload. Its package-private decoder validates exact length/checksum/metadata and never invokes OpenAL or stb_vorbis. Source metadata schema v1, manifest v1, MESH `SMES`, TEXTURE `STEX`, and other pass-through asset types remain unchanged. P6-T09 adds no runtime audio/resource API, OpenAL playback, PCM persistence, transcoding, resampling, streaming policy, spatialization, loop/loudness metadata, dependency graph, or P6-T10+ behavior. | Phase 6 requires a validated source-format boundary for AUDIO before runtime resource loading. Reusing the already present `lwjgl-stb` dependency satisfies the scope-locked stb_vorbis choice without a new dependency family. Explicit whole-memory decoder lifecycle uses stb_vorbis's purpose-built open/info/sample/error/close validation seam while preserving validated compressed Vorbis bytes avoids storage expansion and avoids prematurely deciding whole-clip versus streaming runtime decode policy, while explicit `SAUD` metadata/checksum makes later runtime consumption source-path-independent and corruption-detectable. |

| D-076 | Accepted | P6-T10 / Issue #390 defines a package-private offline asset dependency graph without changing D-068 source metadata v1. Optional adjacent `<source>.deps.json` schema v1 sidecars exist only for MATERIAL, PREFAB, and SCENE owners and contain exactly `schemaVersion`, canonical `assetDependencies`, and canonical opaque `shaderDependencies`. MATERIAL asset edges may target existing TEXTURE AssetIds only and may declare logical shader keys matching `[a-z][a-z0-9._-]{0,127}`; PREFAB/SCENE may target existing AssetIds and may not declare shader keys. No SHADER `AssetType` or public shader asset API is created. Self/missing/type-invalid references, duplicate dependencies, unsupported/orphan sidecars, and AssetId cycles fail before output creation. A deterministic reverse graph returns canonical AssetId-sorted transitive dependents for changed assets or logical shader keys. Successful cooks additionally write deterministic UTF-8 `dependencies.json` schema v1 with owner entries sorted by AssetId and canonical dependency arrays; manifest v1 and cooked payload formats remain unchanged. P6-T10 does not mutate an existing cache or implement incremental recooking. | The backlog requires material texture/shader and scene/prefab dependency invalidation, but D-068 intentionally excludes SHADER from the stable public asset-type schema and D-052 selected no shader asset/public shader API. A separate strict dependency sidecar preserves the accepted metadata contract, while opaque logical shader keys represent the existing renderer concept without raw file paths or a new asset identity. Persisting the validated graph makes exact affected-dependent closure available to later recooking/runtime work without prematurely implementing those systems. |

| D-077 | Accepted | P6-T11 / Issue #393 introduces public typed `ResourceHandle<T>` plus `ResourceHandleState` in `engine-assets`. A handle exposes only AssetId, observable LOADING/READY/FAILED/RELEASED state, optional/required access to the typed Java value, and idempotent local release through AutoCloseable. Public consumers cannot complete a handle and no raw numeric/native/LWJGL/OpenGL/OpenAL handle or internal controller appears in the API. One package-private synchronized `ResourceHandleCell<T>` owns internal LOADING->READY/FAILED completion, rejects null/duplicate/terminal completion without mutation, clears any retained ready value on RELEASED, and makes RELEASED terminal. P6-T11 does not define runtime manifest/file loading, cache identity, reference counting, fallback resources, async execution, native destruction, GPU/OpenAL upload, renderer/world submission, or structured failure payloads. | Phase 6 needs a typed gameplay-facing lifetime boundary before runtime loading/cache policy exists. Separating a read-only generic public view from an internal completion cell prevents gameplay from mutating resource state or receiving backend/native IDs, while idempotent local release gives callers an explicit lifetime action without prematurely claiming reference-counted native ownership. |

## Adding or changing a decision

An Issue must explicitly authorize a durable architecture change. Add a new row with its evidence/gate, update `docs/ARCHITECTURE.md` where structure changes, and reconcile `ENGINE_SCOPE.md` when the product boundary changes. Never rewrite an old decision to hide history; mark it `Superseded` and add the replacement.

When a new/superseding decision changes a public engine API or caller-visible lifecycle, ownership, threading, failure, configuration, or usage semantics, update the relevant [`../wiki/`](../wiki/README.md) pages in the same PR. The decision log remains authoritative; the wiki explains how to consume the accepted implementation and must not create or supersede decisions by itself. If the decision has no consumer/API impact, record `Wiki impact: none — <reason>` instead of editing the wiki unnecessarily.
