package com.samo.engine.assets.api;

/** Stable structured categories for runtime asset-resolution failures. */
public enum AssetLoadErrorCode {
    MISSING_CONTENT, READ_FAILED, INVALID_CONTENT, HOT_RELOAD_FAILED
}
