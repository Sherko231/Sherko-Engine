package com.samo.engine.assets.api;

/** Source asset categories supported by metadata schema version 1. */
public enum AssetType {
    MESH, TEXTURE, MATERIAL, SKELETON, ANIMATION, AUDIO, PREFAB, SCENE
}
